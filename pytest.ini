[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --tb=short
    --color=yes
markers =
    smoke: Smoke tests
    regression: Regression tests
    login: Login tests
    chrome: Tests for Chrome browser
    firefox: Tests for Firefox browser