# 🧪 SauceDemo Test Automation

![Python](https://img.shields.io/badge/Python-3.10+-blue.svg)
![Selenium](https://img.shields.io/badge/Selenium-4.15.0-green.svg)
![Pytest](https://img.shields.io/badge/Pytest-7.4.3-orange.svg)
![Allure](https://img.shields.io/badge/Allure-2.13.0-red.svg)
![Docker](https://img.shields.io/badge/Docker-Enabled-blue.svg)

Проект для автоматизированного тестирования функционала логина на демо-сайте [SauceDemo](https://www.saucedemo.com/). Включает 5 тестовых сценариев, реализованных с использованием современных практик автоматизации тестирования.

## ✨ Особенности

- 🚀 **Автоматическое управление браузером** через Selenium WebDriver
- 🏗 **Паттерн Page Object Model** для удобства поддержки и читаемости кода
- 📊 **Детальные отчеты** с использованием Allure Framework
- 🐳 **Полная поддержка Docker** для изолированного запуска тестов
- 🔧 **Автоматическая установка драйверов** через webdriver-manager
- 📱 **Кросс-браузерное тестирование** (Chrome, Firefox)
- ⚡ **Параллельный запуск тестов** для ускорения выполнения

## 🧪 Тестовые сценарии

| № | Сценарий | Пользователь | Пароль | Ожидаемый результат |
|---|----------|--------------|--------|---------------------|
| 1 | Успешный логин | `standard_user` | `secret_sauce` | Переход на страницу инвентаря |
| 2 | Логин с неверным паролем | `standard_user` | `wrong_password` | Сообщение об ошибке |
| 3 | Логин заблокированного пользователя | `locked_out_user` | `secret_sauce` | Сообщение о блокировке |
| 4 | Логин с пустыми полями | (пусто) | (пусто) | Сообщение о необходимости ввода |
| 5 | Логин с проблемами производительности | `performance_glitch_user` | `secret_sauce` | Успешный вход с задержкой |

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10 или выше
- Google Chrome или Firefox
- Git

### Установка

1. **Клонируйте репозиторий:**
   ```
   git clone https://github.com/ProblemStaf/saucedemo-test-automation.git
   cd saucedemo-test-automation
Создайте виртуальное окружение (рекомендуется):
bash
python3 -m venv venv
source venv/bin/activate  
Установите зависимости:
bash
pip install -r requirements.txt
Проверьте установку Chrome (опционально):
bash
python check_chrome.py
▶️ Запуск тестов

Базовый запуск

bash
# Запуск всех тестов
pytest tests/test_login.py

# Запуск с подробным выводом
pytest tests/test_login.py -v

# Запуск конкретного теста
pytest tests/test_login.py::TestLogin::test_successful_login -v
Запуск с различными браузерами

bash
# По умолчанию используется Chrome
pytest --browser=chrome

# Запуск с Firefox
pytest --browser=firefox

# Headless режим
export HEADLESS=true
pytest tests/test_login.py
📊 Генерация отчетов

# Запустите тесты с генерацией данных для отчета
pytest tests/test_login.py --alluredir=allure-results

# Сгенерируйте и откройте отчет
allure serve allure-results

# Или создайте статический отчет
allure generate allure-results -o allure-report --clean
allure open allure-report
HTML отчеты

bash
# Запустите тесты с генерацией HTML отчета
pytest tests/test_login.py --html=report.html --self-contained-html

# Откройте отчет
open report.html 
🐳 Запуск в Docker

С использованием Docker Compose (рекомендуется)

bash
# Запустите тесты в контейнере
docker-compose up --build

# Запустите в фоновом режиме
docker-compose up -d --build

# Просмотрите логи
docker-compose logs -f

# Остановите контейнеры
docker-compose down
С использованием Dockerfile

bash
# Соберите образ
docker build -t saucedemo-tests .

# Запустите тесты
docker run --rm saucedemo-tests

# Запустите с сохранением результатов Allure
docker run --rm -v $(pwd)/allure-results:/app/allure-results saucedemo-tests
📁 Структура проекта

text
saucedemo-test-automation/
├── pages/                    # Page Object классы
│   ├── __init__.py
│   └── login_page.py        # Страница логина
├── tests/                   # Тестовые сценарии
│   ├── __init__.py
│   └── test_login.py       # Основные тесты
├── utils/                  # Вспомогательные утилиты
│   ├── __init__.py
│   └── driver_setup.py    # Настройка драйвера
├── conftest.py            # Фикстуры pytest
├── requirements.txt       # Зависимости Python
├── Dockerfile            # Конфигурация Docker
├── docker-compose.yml    # Конфигурация Docker Compose
├── pytest.ini           # Конфигурация pytest
├── check_chrome.py      # Скрипт проверки Chrome
├── run_simple.py        # Простой запуск тестов
├── run_tests_with_allure.py # Скрипт запуска с Allure
└── README.md            # Документация
🛠️ Используемые технологии

Python 3.10 - основной язык программирования
Selenium 4 - автоматизация веб-браузера
Pytest - фреймворк для тестирования
Allure - система отчетности
Page Object Model - паттерн проектирования тестов
Docker - контейнеризация тестов
webdriver-manager - автоматическое управление драйверами
🔧 Конфигурация

# Параллельный запуск тестов
pytest -n auto

# Запуск только smoke тестов
pytest -m smoke

# Запуск с кастомным маркером
pytest -m "login and not slow"

Быстрые команды для ежедневного использования

Создайте файл Makefile для удобства:

makefile
.PHONY: test test-allure test-html clean install

install:
	pip install -r requirements.txt

test:
	python -m pytest tests/test_login.py -v

test-allure:
	python -m pytest tests/test_login.py --alluredir=allure-results
	allure serve allure-results

test-html:
	python -m pytest tests/test_login.py --html=report.html --self-contained-html
	open report.html

test-docker:
	docker build -t saucedemo-tests .
	docker run --rm saucedemo-tests

clean:
	rm -rf allure-results allure-report report.html .pytest_cache __pycache__ screenshots

check:
	python check_chrome.py

help:
	@echo "Доступные команды:"
	@echo "  make install     - установить зависимости"
	@echo "  make test        - запустить тесты без отчета"
	@echo "  make test-allure - запустить тесты с Allure отчетом"
	@echo "  make test-html   - запустить тесты с HTML отчетом"
	@echo "  make test-docker - запустить тесты в Docker"
	@echo "  make clean       - очистить временные файлы"
	@echo "  make check       - проверить окружение"
Теперь можно использовать:

bash
make install      # Установить зависимости
make test         # Запустить тесты
make test-allure  # Запустить тесты с Allure
make clean        # Очистить временные файлы

📄 Лицензия

Этот проект распространяется под лицензией MIT. Подробнее см. в файле LICENSE.

👥 Авторы

Ваше Имя - @ProblemStaf
🙏 Благодарности

SauceDemo за предоставление демо-сайта для тестирования
Сообщество Selenium за отличный инструмент для автоматизации
Разработчикам Allure за мощную систему отчетности

Запуск через run.sh
chmod +x run.sh
./run.sh
